<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/products/<%= foundProduct._id %>?_method=PUT" method="POST">
        <label for="name">Product Name</label>
        <input type="text" name="name" placeholder="product name", id="name" value="<%= foundProduct.name %>">
        <label for="price">Product Price</label>
        <input type="number" name="price" placeholder="product price" id="price" value="<%= foundProduct.price%>">
        
        
        
        <label for="category">Select Category</label>
        <select name="category" id="category">
            <% for (let category of categories) { %> 
                <option value="<%= category %>" <%= foundProduct.category ===category ? "selected" : ""%> ><%= category %> </option>
            <% } %> 
        </select>
        
        <label for="onsale">select Onsale</label>
        <select name="onSale" id="onsale">
            <% for (let onSale of onSales) {%> 
            <option value="<%= onSale %>" <%= foundProduct.onSale === onSale ? "selected" : "" %> ><%= onSale %> </option>
            <% } %> 
        </select>
        
        <label for="qty">Product Qty</label>
        <input type="number" name="qty" placeholder="product qty" id="qty" value="<%= foundProduct.qty %>">
        <button>Submit</button>
    </form>
    <a href="/products/<%= foundProduct._id%>">Cancel</a>
    <a href="/products">Back to All Product</a>
</body>
</html>